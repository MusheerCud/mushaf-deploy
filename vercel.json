{
  "buildCommand": "npx nx build mushaf_frontend && mkdir -p ./dist && cp -r ../../../dist/apps/frontend/* ./dist/",
  "outputDirectory": "dist",
  "installCommand": "corepack yarn install --no-immutable"
}
